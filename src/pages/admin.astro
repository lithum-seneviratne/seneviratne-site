---
import Layout from "../components/Layout.astro";

export const prerender = false

const pageTitle = "Admin";

---

<Layout pageTitle={pageTitle}>
    <main class="flex grow justify-center items-center text-ink-dim min-h-screen">
        <p>Youre in!</p>
    </main>
</Layout>